# PhotoLog PRD (Product Requirements Document)

> 이 문서는 Claude Code와 협업할 때 컨텍스트로 사용한다.
> 코드 작성 전에 반드시 이 문서를 먼저 읽고 시작할 것.
> AI 주의사항: 코드를 작성하기 전에 반드시 구현 전략과 이유를 먼저 설명하고, 내 승인을 받은 후에 코드를 수정할 것

---

## 1. 서비스 개요

**서비스명**: PhotoLog  
**한 줄 정의**: 사진을 중심으로 기억을 구조화하는 웹 기반 개인 아카이브 서비스  
**핵심 가치**: 단순한 사진 모음이 아닌, 하나의 결과물로 정리되었다는 느낌

### 타겟 사용자

- 사진 찍는 것을 좋아하고, 여행이나 특별한 순간을 사진과 함께 정리해두고 싶은 사람
- 단순히 찍고 끝나는 것이 아니라, 사진을 중심으로 기억을 구조화하고 싶은 사용자

### 주요 특징

- 회원 전용 서비스 (비회원 접근 불가)
- 웹 환경 중심 (사진 정리/구조화 목적)
- 기록 단위 = 날짜가 아닌 **이벤트** (사진으로 남기고 싶은 순간 하나)
- 날짜는 핵심 데이터가 아닌 탐색을 위한 메타데이터로 사용

---

## 2. 기술 스택

| 영역                    | 기술                 | 선택 이유                                       |
| ----------------------- | -------------------- | ----------------------------------------------- |
| 프레임워크              | Next.js (App Router) | SSR/CSR 전략 분리, SEO 대응                     |
| 언어                    | TypeScript           | 타입 안정성                                     |
| 서버 상태 관리          | React Query          | 서버 상태/UI 상태 명확한 분리                   |
| 스타일링                | Tailwind CSS         | 빠른 UI 구현                                    |
| 백엔드/DB/인증/스토리지 | Supabase             | BaaS로 백엔드 복잡도 낮춤, 이미지 스토리지 내장 |
| 패키지 매니저           | pnpm                 | npm 대비 속도 빠름, 유령 의존성 방지            |
| 배포                    | Vercel (예정)        | Next.js 최적화 환경                             |

---

## 3. 개발 환경 설정

### 코드 품질 도구

- **ESLint**: 코드 오류 및 안티패턴 감지
- **Prettier**: 코드 포맷 자동화
  - `semi: false`, `singleQuote: true`
  - 저장 시 미사용 import 자동 제거 및 정렬

### 폰트

- **Pretendard** (가변 폰트)
- `next/font/local`로 self-hosting
  - 외부 서버 요청 없이 빠른 로딩
  - CLS(레이아웃 시프트) 방지
  - CSS 변수(`--font-sans`)로 전역 관리

### 브랜치 전략

- `main`: 안정된 환경 세팅 보존 (복구 지점)
- `dev`: 기능 개발 작업 브랜치
- 기능 완성 후 `dev` → `main` 머지

### 커밋 컨벤션

| 접두어   | 의미                           |
| -------- | ------------------------------ |
| feat     | 새로운 기능 추가               |
| fix      | 버그 수정                      |
| docs     | 문서 수정                      |
| style    | 포맷팅 등 코드 변경 없는 경우  |
| refactor | 코드 리팩토링                  |
| chore    | 빌드, 패키지 매니저, 환경 설정 |

---

## 4. 폴더 구조

> 컴포넌트/유틸 등을 `src/` 최상단에 두지 않고, 전부 `src/app/` 하위에 두는 구조로 진행한다.
> (Next.js App Router 기준 co-location)

```
src/app/
├── components/
│   ├── common/       # Button, ImagePicker 등 범용 컴포넌트
│   └── layout/       # Header, Footer 등 레이아웃 컴포넌트
├── layout.tsx        # 루트 레이아웃 (폰트, Header/Footer)
├── page.tsx          # 메인(랜딩) 페이지
├── globals.css       # 글로벌 스타일
├── write/            # 기록 작성 페이지
├── archive/          # 기록 목록 페이지 (예정)
├── post/[id]/        # 기록 상세 페이지 (예정)
├── community/        # 커뮤니티 페이지 (예정)
└── mypage/           # 마이페이지 (예정)
```

> `constants/`, `hooks/`, `types/`, `utils/`는 아직 생성되지 않았다.
> 필요해지는 시점에 `src/app/` 하위에 추가한다.

---

## 5. 페이지 구성 및 렌더링 전략

| 페이지                     | 렌더링 전략       | 이유                                              |
| -------------------------- | ----------------- | ------------------------------------------------- |
| 메인(랜딩) 페이지          | SSG or SSR        | 공개 기록 노출, SEO 목적                          |
| 기록 목록 페이지 (Archive) | CSR + React Query | 로그인 후 접근, URL 탐색 조건에 따라 데이터 변동  |
| 기록 상세 페이지           | SSR               | 공개 설정 시 외부 공유 가능, 메타데이터 생성 목적 |
| 기록 작성 페이지           | CSR               | 사용자 인터랙션 중심, 실시간 상태 변화            |
| 마이페이지                 | CSR               | 사용자 인터랙션 중심                              |
| 커뮤니티 페이지            | CSR or SSR        | 공개된 타인 기록 열람 (읽기 전용)                 |

> **설계 의도**: 페이지 성격에 따라 렌더링 전략을 의도적으로 선택했다.
> "왜 이 페이지는 SSR인지, 왜 저 페이지는 CSR인지" 면접에서 설명 가능해야 한다.

---

## 6. 상태 관리 기준

상태를 3가지로 명확히 분리한다.

| 상태 종류 | 관리 방식        | 예시                                    |
| --------- | ---------------- | --------------------------------------- |
| 서버 상태 | React Query      | 기록 목록, 기록 상세, 사용자 정보       |
| UI 상태   | useState (로컬)  | 입력값, 모달 열림/닫힘, 미리보기 이미지 |
| URL 상태  | URL Query String | 날짜 필터, 태그, 정렬 기준              |

> **React Query를 쓰는 이유**:
> 데이터를 가져오기 위해서가 아니라,
> 서버 상태와 UI 상태를 명확히 분리하고
> 기록 탐색 흐름을 예측 가능하게 만들기 위해서다.

---

## 7. 인증 구조

- 인증 방식: Supabase Auth (세션 기반)
- 새로고침/재방문 시 로그인 상태 유지
- 프론트에서 인증 상태는 **서버 상태로 관리** (Supabase `getUser()` 기반)
- 로그인하지 않으면 기록 목록/작성/마이페이지 접근 불가
- 예외: 공개 설정된 기록 상세 페이지는 비로그인도 열람 가능

### 권한 구분

| 대상      | 내 기록 | 타인 공개 기록 | 타인 비공개 기록 |
| --------- | ------- | -------------- | ---------------- |
| 조회      | ✅      | ✅             | ❌               |
| 수정/삭제 | ✅      | ❌             | ❌               |

---

## 8. Storage 설계

### 버킷

- 이름: `post-images`
- Public: **OFF (private)**
  - 이유: 버킷을 public으로 두면 URL만 알아도 이미지에 접근 가능해져, "타인 비공개 기록은 조회 불가"라는 7번 권한 구분과 어긋남
- 경로 규칙: `{user_id}/{파일명}`
  - RLS 정책에서 경로의 첫 세그먼트를 `auth.uid()`와 비교해 소유자를 판별하기 위함

### RLS 정책 (owner-only)

현재는 업로드한 본인만 자신의 파일을 조회/수정/삭제할 수 있도록 제한한다.
"공개로 설정한 기록의 이미지를 타인도 볼 수 있게" 하는 부분은 posts 테이블 설계 이후, SSR인 기록 상세 페이지(5번 렌더링 전략 참고)에서 signed URL을 발급하는 방식으로 확장할 예정이며, 버킷 자체는 계속 private로 유지한다.

```sql
-- 업로드 (본인 폴더에만)
create policy "post-images insert own folder"
on storage.objects for insert
to authenticated
with check (
  bucket_id = 'post-images'
  and (storage.foldername(name))[1] = auth.uid()::text
);

-- 조회 (본인 것만)
create policy "post-images select own folder"
on storage.objects for select
to authenticated
using (
  bucket_id = 'post-images'
  and (storage.foldername(name))[1] = auth.uid()::text
);

-- 수정
create policy "post-images update own folder"
on storage.objects for update
to authenticated
using (
  bucket_id = 'post-images'
  and (storage.foldername(name))[1] = auth.uid()::text
);

-- 삭제
create policy "post-images delete own folder"
on storage.objects for delete
to authenticated
using (
  bucket_id = 'post-images'
  and (storage.foldername(name))[1] = auth.uid()::text
);
```

---

## 9. 기록(Post) 스펙

### 기록 단위 정의

- 날짜 일기가 아닌 **이벤트** 단위
- 예: "제주도 여행", "친구 생일", "첫 출근날"

### 기록 구성 요소

- 사진: 최소 1장, 최대 5장 (MVP에서는 사진 필수)
- 텍스트: 제목 + 본문
- 태그: 탐색을 위한 메타데이터
- 날짜: 탐색을 위한 메타데이터 (핵심 데이터 아님)
- 공개/비공개 설정

### 사진 업로드 UX

- 업로드 진행 상태 표시 필요
- 미리보기 먼저 표시
- 업로드 실패 시 해당 사진만 재시도 가능 (전체 작성 막으면 안 됨)
- 저장 완료 후 → 기록 상세 페이지로 이동

---

## 10. MVP 범위

### ✅ 완료

- Next.js 초기 세팅 + ESLint/Prettier 설정
- pnpm 패키지 매니저 설정
- Pretendard 폰트 전역 설정
- 깃허브 레포 + 브랜치 전략 (main/dev)
- 디자인 시안
- 기획 문서 (이 PRD)
- 메인(랜딩) 페이지
- 글 작성 페이지 (UI)
- 헤더/푸터/버튼 공통 컴포넌트
- 반응형 설정

### 🔄 진행 중

- 이미지 업로드 (Supabase Storage 연결 필요)

### ⬜ 시작 전

- DB 설계 (Supabase 테이블/RLS)
- 인증 흐름 (로그인/로그아웃/세션 유지)
- 글 목록 페이지 (URL 상태 기반 필터/정렬)
- 글 상세 페이지 (SSR + 메타데이터)
- 공개/비공개 권한 처리 (RLS)
- 커뮤니티 페이지
- 마이페이지
- 배포 (Vercel)

### MVP 제외 (추후 확장)

- 타임라인 뷰
- 지도 뷰
- (단, 동일한 데이터로 확장 가능하도록 구조는 열어둠)

---

## 11. SEO / 공유

- 개인 접근 중심 서비스 (MVP)
- 외부 공유가 의미 있는 페이지: 공개 설정된 기록 상세 페이지
- 해당 페이지에서 메타데이터 제공: 제목, 요약 설명, 대표 이미지

---

## 12. Claude Code 협업 규칙

1. **코드 먼저 짜달라고 하지 않는다.**
   내가 먼저 생각하고 → Claude Code에게 피드백 요청하는 순서로 진행한다.

2. **모르는 부분은 "왜?"를 3번 물어본다.**
   - 이게 뭘 하는 코드야? (동작)
   - 왜 이 방식을 썼어? (다른 방법은 없었나)
   - 이게 없으면 무슨 문제가 생겨? (필요성)

3. **기능 하나 끝낼 때마다 회고를 남긴다.**
   - 배운 개념 / 헷갈렸던 부분 / 왜 이렇게 결론났는지

4. **이해 안 된 채로 다음 단계로 넘어가지 않는다.**

---

## 13. Claude Code 시작 방법

### 새 대화 시작할 때마다

```
PRD.md 읽어줘. 오늘은 [작업 내용]부터 시작할 건데,
내가 먼저 생각해볼게 — 피드백해줘.
```

### 현재 코드 파악이 필요할 때

```
PRD.md 읽고, 현재 src 폴더 구조랑 코드 파악해줘.
완료된 것과 미완성인 것 정리해줘.
```
